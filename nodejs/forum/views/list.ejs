<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/main.css" />
  </head>
  <body>
   <%- include('nav.ejs',{age:20}) %>     
   <!-- 특수한 문법사용 -->
    <div class="listbox">
      <% for(let i=0;i<posts.length; i++){ %> 
        <!-- html에 자바스크립 문법쓸 때 -->
      <div class="list1">
        <h4><%= posts[i].title %></h4>
        <!-- =는 안에 있는 html그냥 문자로 나옴, -는 html로 렌더링되서나옴 -->
        <p><%= posts[i].content %></p>
      </div>
    <% } %>
      
    </div>
  </body>
</html>
